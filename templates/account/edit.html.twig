{% extends 'frontend/layouts/base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}

<div class="container">
<div class="row">
<div class="px-lg-4 col-lg-6 mx-auto">
<div class="card">
    <h1>Edition compte</h1>

    {{ form_start(form) }}
    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.email) }}

    <input class="btn btn-primary" type="submit" value="{{ submitButtonText|default('Editer account') }}" formnovalidate>
    {{ form_end(form) }}

</div>
</div>
</div>
</div>
{% endblock %}