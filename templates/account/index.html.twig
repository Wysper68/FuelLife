{% extends 'frontend/layouts/base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}

<div class="container">
<div class="row">
<div class="px-lg-4 col-lg-6 mx-auto">
<div class="card">
    <h1>Mon compte </h1>

    <p>{{ app.user.email }}</p>
    <p>{{ app.user.FullName }}</p>

    <a href="{{ path('app_account_edit')  }}">Editer infos</a>
    <a href="{{ path('app_change_password')  }}">Changer le mot de passe</a>


</div>
</div>
</div>
</div>
{% endblock %}