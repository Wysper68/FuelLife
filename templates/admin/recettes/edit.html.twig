{% extends 'admin/layouts/base.html.twig' %}

{% block body %}
    <h1>Edit Recette</h1>

    {{ include('admin/recettes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_recettes_index') }}">back to list</a>

    {{ include('admin/recettes/_delete_form.html.twig') }}
{% endblock %}
